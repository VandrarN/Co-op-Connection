name: Build and deploy Co-op Connection

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install pygbag
        run: pip install pygbag

      - name: Build game
        run: python -m pygbag --build --html --width 900 --height 1350 --ume_block 0 --CONSOLE 0 .

      - name: Show build output
        run: find build -type f | sort

      - name: Fix archive name
        run: |
          ARCHIVE_FILE=$(find build/web -type f \( -name "*.tar.gz" -o -name "*.apk" -o -name "*.zip" \) | head -n 1)
          echo "Found archive: $ARCHIVE_FILE"

          if [ -z "$ARCHIVE_FILE" ]; then
            echo "No archive file found in build/web"
            find build/web -type f | sort
            exit 1
          fi

          cp "$ARCHIVE_FILE" build/web/co-op-connection.tar.gz

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages

    steps:
      - name: Deploy
        uses: actions/deploy-pages@v4
